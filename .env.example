# RSS Feed Summarizer Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED: OpenAI API Configuration
# =============================================================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAIAPIKEY=your_openai_api_key_here

# =============================================================================
# EMAIL CONFIGURATION (Optional - for email distribution)
# =============================================================================
# Gmail SMTP Configuration
# 1. Use your Gmail address (e.g., yourname@gmail.com)
# 2. You need to create a Gmail App Password (not your regular password)
# 3. Go to your Google Account: https://myaccount.google.com/
# 4. Go to Security > 2-Step Verification (must be enabled first)
# 5. Scroll down to "App passwords" and click it
# 6. Select "Mail" as the app and "Other" as the device (name it "RSS Summarizer")
# 7. Click "Generate" and copy the 16-character password
# 8. Paste it below (no spaces)
SMTP_USER=your_email@gmail.com
GMAIL_APP_PASSWORD=your_gmail_app_password_here

# Email recipients (comma-separated list) - Leave empty to use Google Sheets subscribers
EMAIL_RECIPIENTS=recipient1@example.com,recipient2@example.com

# =============================================================================
# GOOGLE SHEETS CONFIGURATION (Required for email subscriptions)
# =============================================================================
# 1. Go to Google Cloud Console (https://console.cloud.google.com/)
# 2. Create a new project or select existing one
# 3. Enable Google Sheets API and Google Drive API
# 4. Go to Service Accounts > Create Service Account
# 5. Download the JSON key file
# 6. Copy the entire JSON content below (can be multi-line for readability)
GOOGLE_SHEETS_CREDENTIALS={
  "type": "service_account",
  "project_id": "your-project",
  "private_key_id": "...",
  "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n",
  "client_email": "your-service-account@your-project.iam.gserviceaccount.com",
  "client_id": "...",
  "auth_uri": "https://accounts.google.com/o/oauth2/auth",
  "token_uri": "https://oauth2.googleapis.com/token",
  "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
  "client_x509_cert_url": "...",
  "universe_domain": "googleapis.com"
}

# Google Sheet ID (from the URL: https://docs.google.com/spreadsheets/d/SHEET_ID/edit)
# 1. Create a new Google Sheet
# 2. Share it with your service account email (from GOOGLE_SHEETS_CREDENTIALS)
# 3. Give it Editor permissions
# 4. Copy the Sheet ID from the URL
SHEET_ID=your_google_sheet_id_here

